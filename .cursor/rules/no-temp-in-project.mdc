---
description: Do not create temporary files or directories inside the project
alwaysApply: true
---

# Temporary files and directories

- Не создавать временные каталоги/файлы (например `tmp_epf_src`, `tmp`, `test_output`) внутри проекта.
- Для временных файлов: если в корне проекта есть junction `.temp/` — писать туда; иначе — системный temp (`tempfile.mkdtemp()`, `tempfile.TemporaryDirectory()`). См. `workspace-junctions.mdc`.
- Результаты преобразований (разбора, конвертации) писать только во временный каталог вне проекта, никогда в `tests/data/`.
